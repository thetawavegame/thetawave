use bevy::hierarchy::ChildBuilder;
/*
pub(super) trait UIChildBuilderExt {
    //fn spawn_player_join_row();

    fn spawn_player_join(&mut self);
}

impl UIChildBuilderExt for ChildBuilder<'_> {
    fn spawn_player_join(&mut self) {
        self.spawn()
    }
}
*/
